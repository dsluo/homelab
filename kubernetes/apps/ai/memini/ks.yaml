---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app memini
spec:
  interval: 1h
  path: ./kubernetes/apps/ai/memini/app
  components:
    - ../../../../components/database
    - ../../../../components/litellm-key
  dependsOn:
    - name: external-secrets-config
      namespace: external-secrets
    - name: llmkube-models
    - name: litellm
  postBuild:
    substitute:
      APP: *app
      # memini's postgres backend is VectorChord, not plain pgvector, and its
      # own CREATE EXTENSION runs as the unprivileged owner role — so CNPG has
      # to have created both before the first boot.
      DB_EXTENSIONS: '[{"name":"vector"},{"name":"vchord"}]'
      LITELLM_KEY_MODELS: '"llmkube/Qwen/Qwen3-Embedding-0.6B", "llmkube/Qwen/Qwen3-Reranker-0.6B"'
    substituteFrom:
      - name: cluster-global-config
        kind: ConfigMap
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: ai
  wait: false
