---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kanidm-tls
spec:
  secretName: kanidm-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames: ["idm.${SECRET_DOMAIN}"]
