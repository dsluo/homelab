---
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: litellm-operator
spec:
  interval: 1h
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  ref:
    # TEMPORARY fork of upstream, built from feat/proxy-metrics: adds
    # spec.metrics to LiteLLMProxy, which is what renders the ServiceMonitor and
    # the prometheus callback ../proxy/proxy.yaml asks for. The chart also pins
    # image.repository/digest to the matching build, so nothing is overridden in
    # ./helmrelease.yaml. Point both back at home-operations once the change is
    # released upstream.
    tag: 0.0.20-dev.fa52142
  url: oci://ghcr.io/dsluo/charts/litellm-operator
