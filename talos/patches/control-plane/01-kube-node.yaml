---
apiVersion: v1alpha1
kind: KubeNodeConfig
labels:
  # Would pull talos0 out of LoadBalancer endpoint selection; it carries pods.
  node.kubernetes.io/exclude-from-external-load-balancers:
    $patch: delete
taints:
  # Was cluster.allowSchedulingOnControlPlanes: true.
  node-role.kubernetes.io/control-plane:
    $patch: delete
