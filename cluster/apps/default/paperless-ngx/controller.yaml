apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: paperless-ngx
  namespace: flux-system
spec:
  interval: 10m0s
  chart:
    spec:
      chart: ./cluster/apps/default/paperless-ngx/app
      sourceRef:
        kind: GitRepository
        name: flux-system
