---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app kepler
spec:
  chartRef:
    kind: OCIRepository
    name: *app
  interval: 1h
  values:
    serviceMonitor:
      enabled: true
