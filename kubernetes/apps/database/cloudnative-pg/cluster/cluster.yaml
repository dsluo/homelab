---
# yaml-language-server: $schema=https://datreeio.github.io/CRDs-catalog/postgresql.cnpg.io/cluster_v1.json
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgres
  annotations:
    cnpg.io/skipEmptyWalArchiveCheck: enabled
spec:
  instances: 1
  # single instance on node-local storage: the PDB can never be satisfied (no
  # failover target), so it would deadlock node drains during tuppr upgrades.
  enablePDB: false
  imageName: ghcr.io/cloudnative-pg/postgresql:18@sha256:21497884139dc18c9015e11f02a28a28962d9f670a42d9bf4481b7fd37b5213c

  primaryUpdateStrategy: unsupervised
  # switch to switchover when instances > 1
  primaryUpdateMethod: restart

  storage:
    storageClass: openebs-hostpath
    size: 20Gi

  managed:
    services:
      # we're single node anyway
      disabledDefaultServices: ["ro", "r"]
    roles:
      - name: &default app
        login: false
      - name: paperless
        ensure: present
        login: true
        passwordSecret:
          name: paperless-postgres-secret
      - name: sonarr
        ensure: present
        login: true
        passwordSecret:
          name: sonarr-postgres-secret
      - name: radarr
        ensure: present
        login: true
        passwordSecret:
          name: radarr-postgres-secret
      - name: prowlarr
        ensure: present
        login: true
        passwordSecret:
          name: prowlarr-postgres-secret
      - name: pocketid
        ensure: present
        login: true
        passwordSecret:
          name: pocketid-postgres-secret
      - name: seerr
        ensure: present
        login: true
        passwordSecret:
          name: seerr-postgres-secret

  plugins:
    - name: barman-cloud.cloudnative-pg.io
      enabled: true
      isWALArchiver: true
      parameters:
        barmanObjectName: garage

  bootstrap:
    # initdb:
    #   database: *default
    #   owner: *default
    recovery:
      database: *default
      owner: *default
      source: garage

  externalClusters:
    - name: garage
      plugin:
        name: barman-cloud.cloudnative-pg.io
        parameters:
          barmanObjectName: garage
          serverName: postgres

  resources:
    requests:
      cpu: 500m
      memory: 512Mi
    limits:
      memory: 1Gi

  monitoring:
    enablePodMonitor: true
