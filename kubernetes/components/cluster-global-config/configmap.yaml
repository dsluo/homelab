---
# Plain resource, not a generator: Flux's substituteFrom needs a stable name.
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-global-config
data:
  DOMAIN: crayon.club
  # Overwritten per namespace by the replacement in ./kustomization.yaml.
  NAMESPACE: ""
