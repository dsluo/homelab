apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paperless-app-pvc
  namespace: default
spec:
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
