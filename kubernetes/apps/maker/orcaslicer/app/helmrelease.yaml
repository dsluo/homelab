---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app orcaslicer
spec:
  chartRef:
    kind: OCIRepository
    name: *app
  interval: 1h
  values:
    controllers:
      *app :
        annotations:
          reloader.stakater.com/auto: "true"
        pod:
          securityContext:
            fsGroup: 911
            fsGroupChangePolicy: OnRootMismatch
          resourceClaims:
            - name: gpu
              resourceClaimTemplateName: orcaslicer
        containers:
          *app :
            image:
              repository: ghcr.io/linuxserver/orcaslicer
              tag: 2.3.2@sha256:f823bb9055bd63fe7f89fb9d0aad4dcf981f1f1206cec9d1b08f50ccd08784c4
            env:
              TZ: America/Detroit
              PIXELFLUX_WAYLAND: "true"
              DRINODE: &gpu /dev/dri/renderD128
              DRI_NODE: *gpu
              # S6_READ_ONLY_ROOT: "1"
              PUID: 911
              PGID: 911
            probes:
              liveness: &probes
                enabled: true
                spec:
                  failureThreshold: 10
              startup: *probes
              readiness: *probes
            securityContext:
              allowPrivilegeEscalation: false
              # readOnlyRootFilesystem: true
            resources:
              claims:
                - name: gpu
              requests:
                cpu: 100m
                memory: 1Gi
              limits:
                memory: 4Gi

    service:
      *app :
        ports:
          http:
            port: &port 3000
    route:
      *app :
        hostnames:
          - "orcaslicer.${SECRET_DOMAIN}"
        parentRefs:
          - name: envoy-internal
            namespace: network
        rules:
          - backendRefs:
              - identifier: *app
                port: *port
    persistence:
      config:
        existingClaim: *app
        globalMounts:
          - path: /config/.config/OrcaSlicer
            subPath: orcaslicer
          - path: /config/Desktop
            subPath: models
      shm:
        type: emptyDir
        medium: Memory
        sizeLimit: 1Gi
        globalMounts:
          - path: /dev/shm
      tmpfs:
        type: emptyDir
        globalMounts:
          - path: /run
            subPath: run
          - path: /tmp
            subPath: tmp
          - path: /config
            subPath: config
          - path: /config/.config/OrcaSlicer/log
            subPath: log
          - path: /config/.config/OrcaSlicer/cache
            subPath: cache
